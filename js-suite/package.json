{
  "name": "multiprofile-jest-suite",
  "version": "1.0.0",
  "private": true,
  "description": "Small Jest suite used to prove a single Smart Tests workspace can record multiple CLI profiles (pytest + jest) side by side.",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0"
  },
  "jest": {
    "reporters": [
      "default",
      "jest-junit"
    ]
  },
  "jest-junit": {
    "outputDirectory": ".",
    "outputName": "junit.xml"
  }
}
